/* Design-time preview aid. The build strips the <script data-polyfill> tag; this file
   still mirror-copies like any asset. A real polyfill is post-MVP (product spec §6.3). */
console.debug("unify polyfill stub");
