{
  "name": "thistleknap-forge",
  "private": true,
  "type": "module",
  "scripts": {
    "build:calculator": "node _scripts/build-fee-calculator.mjs",
    "build:notes": "node _scripts/gen-notes-index.mjs",
    "publish": "npm run build:calculator && npm run build:notes && ./unify build --clean --pretty-urls --base-url https://thistleknap.pages.dev/forge/"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "esbuild-svelte": "^0.9.5",
    "svelte": "^4.2.20"
  }
}
